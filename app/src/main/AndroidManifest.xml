<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dsandroidhamza_oussama">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DsAndroidHamza_Oussama">
        <activity android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StatisticsActivity" android:theme="@style/SplashScreen"/>
        <activity android:name=".recyclers.RecyclerRankingActivity" android:theme="@style/SplashScreen"/>
        <activity android:name=".recyclers.RecyclerCumulActivity" android:theme="@style/SplashScreen"/>
        <activity android:name=".ResultActivity" android:theme="@style/SplashScreen"/>
        <activity android:name=".ChaptersActivity" />
        <activity android:name=".RateUsActivity" />
        <activity android:name=".QuestionFragmentActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>